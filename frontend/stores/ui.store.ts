/**
 * ui.store.ts — canonical re-export (prompt-spec alias).
 * The implementation lives in ../store/ui.ts.
 */
export { useUIStore } from "@/store/ui";
export type { Toast, ToastVariant } from "@/store/ui";
