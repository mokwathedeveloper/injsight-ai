/**
 * wallet.store.ts — canonical re-export (prompt-spec alias).
 * The implementation lives in ../store/wallet.ts.
 */
export { useWalletStore } from "@/store/wallet";
export type { } from "@/store/wallet";
